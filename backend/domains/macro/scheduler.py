# Macro domain scheduler placeholder.
# Scheduling is handled centrally via the DomainOrchestrator registered
# in main.py (_register_domains).  This module is reserved for any
# future macro-specific scheduling logic (e.g. COT weekly pulls).
